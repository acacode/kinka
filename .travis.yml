language: node_js
node_js:
  - "node"
cache:
  yarn: true
  directories:
      - "node_modules"
before_install:
  - echo "//registry.npmjs.org/:_authToken=\${NPM_TOKEN}" > .npmrc
  - npm i -g github-release-cli
install:
  - yarn
script:
  - yarn lint
  - yarn build
  - yarn compare-versions
after_success:
  - if [ ${TRAVIS_BRANCH} = "master" ] && [ "${TRAVIS_PULL_REQUEST}" = "false" ];then
    node ./bin/git-tag.js;
    node ./bin/prepare-package.js;
    npm publish;
    fi
